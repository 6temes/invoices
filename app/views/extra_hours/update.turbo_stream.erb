<%= turbo_stream.replace dom_id(@extra_hour) do %>
  <%= render "extra_hours/extra_hour_row", client: @client, extra_hour: @extra_hour %>
<% end %>

<%= turbo_stream.prepend "flash_messages" do %>
  <%= render "shared/flash", type: :notice, message: "Extra hours updated." %>
<% end %>
